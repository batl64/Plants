import React, { useState, useEffect } from "react";
import { withTranslate } from "react-redux-multilingual";

const Map = (props) => {
  const { translate } = props;
  return <div className="main-page"></div>;
};

export default withTranslate(Map);
